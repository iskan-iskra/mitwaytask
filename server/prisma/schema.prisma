generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../sqli.db"
}

model DataBase {
  Id     Int    @unique
  Name   String
  Role   String
  Status String
}
